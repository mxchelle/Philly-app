/*Template.AccountModal.events({
	'click .nav-toggle' : ()=>{
		Session.set('nav-toggle', open);
	},
	'click' : function(event, template) {
		if(event.target.className != 'account-modal'){
			Session.set('nav-toggle', close);
		}
	}
});*/